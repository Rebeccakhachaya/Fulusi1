function check(arr){
    let left=arr[0]
    let right=arr[arr.length-1]
for(let i =0;i<left.length; i++){
    if (left.contains(i) && (right.contains(j))){
        console.log(i)
    }
     
}
}
check(["1,3,4,7,13","1,2,4,13,15"])

